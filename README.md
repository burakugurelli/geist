# geist
Geist is a lightweight open-source library for synthesized interface sounds in web and SwiftUI applications. Sounds are generated at runtime by a fast Rust core—without bundled audio files or unnecessary dependencies.
